import 'package:flutter/material.dart';

class SelectionScreenView extends StatelessWidget {
  const SelectionScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Selection Screen"),),
    );
  }
}