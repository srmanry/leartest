import 'package:flutter/material.dart';

class HomeSereenView extends StatelessWidget {
  const HomeSereenView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
